window.onload = function(){
    var playButton = document.getElementById("playButton");
    var playing = false;
    playButton.addEventListener("click", function(){
        tracks
        /*
        if (!playing){
            var tracks = document.getElementsByClassName("track");
            for (track in tracks){
                var aud = track.getElementsByTagName("audio");
                console.log(aud);
                aud.play();
            }
            playing = true;
        }
        else{
            var tracks = document.getElementsByClassName("track");
            for (track in tracks){
                track.audio.stop();
            }
            playing = false;
        }*/
    });
    
};
